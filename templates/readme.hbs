# {{serverName}}

{{description}}

Generated MCP server with {{tools.length}} tools from OpenAPI specification.

## Installation

```bash
npm install
```

## Build

```bash
npm run build
```

## Usage

This is an MCP (Model Context Protocol) server that provides access to the following tools:

{{#each tools}}
### {{name}}

{{description}}

**Method:** `{{method}}` **Path:** `{{path}}`

{{#if inputSchema.properties}}
**Parameters:**
{{#each inputSchema.properties}}
- `{{@key}}` ({{this.type}}){{#if this.description}} - {{this.description}}{{/if}}
{{/each}}
{{/if}}

{{#if authentication}}
**Authentication:** {{authentication.type}}
- Environment variable: `{{authentication.envVariable}}`
{{/if}}

---
{{/each}}

## Environment Variables

{{#each tools}}
{{#if authentication}}
- `{{authentication.envVariable}}` - {{#if authentication.type}}{{authentication.type}} {{/if}}authentication for {{name}}
{{/if}}
{{/each}}

## Running the Server

After building, you can run the server with:

```bash
npm start
```

The server will run on stdio and wait for MCP client connections.

## Development

- `npm run dev` - Build in watch mode
- `npm run lint` - Run linting
- `npm run clean` - Clean build artifacts

## Generated Information

- **Generated at:** {{metadata.generatedAt}}
- **Generator version:** {{metadata.generatorVersion}}
- **Source:** {{metadata.sourceSpec}}

## License

MIT